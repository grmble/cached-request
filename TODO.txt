stale results are served and refreshed in the background (if behaviour is unwanted, only configure ttl)
async http as dev dependency for testing

x (A) only one promise for a given key
x starting cache also returns config along with cache manager
x result is enriched with time of aquisition
